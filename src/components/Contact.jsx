export default function Contact() {
    return(
        <section id="contact">
            <h2>Contatti</h2>
            <p>Email, Githu, Telegram (qui ci metterai le cose)</p>
        </section>
    );
}